"""identity service: users, credentials, OAuth, JWT issuing and RBAC.

Owns users, credentials, roles and permissions; no other service writes them
(ARCHITECTURE section 3)."""

__all__: list[str] = []
