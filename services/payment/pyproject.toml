[project]
name = "payment"
version = "0.1.0"
description = "Stripe integration: authorization, capture and refund."
requires-python = ">=3.12,<3.13"
dependencies = ["chassis"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/payment"]
